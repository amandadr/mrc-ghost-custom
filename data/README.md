# Data (not theme runtime)

Ghost import payloads and builders. Excluded from `yarn zip`.

| Path | Role |
|------|------|
| [ghost-import/](ghost-import/) | Labs JSON + HTML/Markdown drafts for resources posts. Rebuild JSON with `node data/ghost-import/build.mjs` from the theme root. |

Lighthouse dumps live in [docs/archive/perf/](../docs/archive/perf/).
