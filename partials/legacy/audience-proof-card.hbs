{{! Experience / proof card }}
<article class="mrc-audience-proof-card">
  {{#if title}}
    <h3 class="mrc-audience-proof-card__title">{{title}}</h3>
  {{/if}}
  {{#if copy}}
    <p class="mrc-audience-proof-card__copy">{{copy}}</p>
  {{/if}}
</article>
