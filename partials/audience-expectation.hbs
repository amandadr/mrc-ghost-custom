{{! Working-together principle — olive expectations section }}
<div class="mrc-audience-expectation">
  {{#if title}}
    <h3 class="mrc-audience-expectation__title">{{title}}</h3>
  {{/if}}
  {{#if copy}}
    <p class="mrc-audience-expectation__copy">{{copy}}</p>
  {{/if}}
</div>
